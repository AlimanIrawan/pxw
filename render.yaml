services:
  # 免费Web服务 - 包含控制界面和内置定时任务
  - type: web
    name: detik-crawler
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
